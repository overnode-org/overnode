#
# Sample Cade configuration to deploy ElasticSearch service
#
placements:
  default:
    services:
      elasticsearch:
        seeds: 1
        ports:
          9200: 9200

services:
  elasticsearch:
    image: cadeworks/elasticsearch:5.4.0
