# Unique project id. Do not delete this field.
# It is OK to set it to some recognisable name initially.
# Once defined and set, do not edit.
id: dev

# Hint: run the following command to add sample service to the configuration
# > overnode init https://github.com/avkonst/overnode#examples/sleep

# Sourced from: https://github.com/avkonst/overnode/examples/weavescope/overnode.yml
weavescope:
    weavescope.yml: *

# Sourced from: https://github.com/avkonst/overnode/examples/sleep/overnode.yml
sleep:
    # Example stack below
    # Apply the following compose file to all nodes.
    # You can also target specfic nodes, for example: 1-3,6,10
    sleep.yml: *


# Sourced from: https://github.com/avkonst/overnode/examples/kafka/overnode.yml
kafka:
    kafka.yml: *
    # adjust target nodes for the zookeeper instances in the .env file
    zookeeper.yml: ${ZOOKEEPER_NODE_1},${ZOOKEEPER_NODE_2},${ZOOKEEPER_NODE_3}

prometheus:
    cadvisor.yml: *
    prometheus.yml: 2
    grafana.yml: 2
    loki-link.yml: *
    loki.yml: 3
