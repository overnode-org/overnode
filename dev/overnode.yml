# Unique project id. Do not delete this field.
# It is OK to set it to some recognisable name initially.
# Once defined and set, do not edit.
id: dev

# Hint: run the following command to add sample service to the configuration
# > overnode init https://github.com/avkonst/overnode#examples/sleep

kafka:
    kafka.yml: *


# Sourced from: https://github.com/avkonst/overnode/examples/weavescope/overnode.yml
weavescope:
    weavescope.yml: *


# Sourced from: https://github.com/avkonst/overnode/examples/graylog/overnode.yml
influxdb:
    graylog-telegraf.yml: *
    graylog.yml: 1
    graylog-mongo.yml: 1
    graylog-es.yml: 1

